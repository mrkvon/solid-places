import { test } from '@playwright/test'

test.describe('Map of Places', () => {
  test.fixme('can show places on a map', async () => {})
  test.fixme('can filter places (use the same filter as with list)', async () => {})
})
